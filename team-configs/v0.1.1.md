# Team Config v0.1.1

Last updated: 2026-07-13 22:12:38 PDT — edited by: Codex

Status: APPROVED by Mark 2026-07-11 (E1-R1A03, as amended — decisions/2026-07-11-run-001-approvals.md DR-15). Supersedes v0.1.

**Amendment (Mark, 2026-07-11; clarified 2026-07-12):** Claude is **not** the
default provider. Mark manually prompts the agent for each invocation and
names the provider/agent in that prompt. Codex, Claude, and Hermes are all
allowed external provider surfaces for bounded prototype work. Telegram is not
part of this workflow. The data-class rules are unchanged from v0.1.

| role | actor | provider surface | allowed tasks | allowed data classes | fallback |
| --- | --- | --- | --- | --- | --- |
| Exploration/ranking/drafting | Agent/provider manually named by Mark per invocation — Codex, Claude, or Hermes. **No default provider.** Telegram is excluded from the workflow. | Each named agent's provider is an EXTERNAL surface (Codex/OpenAI, Claude/Anthropic, Hermes' default provider) | fill missing hypothesis fields; follow optional research controls; research public sources; surface alternatives/contradictions; rank and draft path cards; review public profiles only when a hypothesis sets `Profile tracking: watch` | a safe Growth Hypothesis in `growth/HYPOTHESES.md` and public sources; NEVER local-only | mark agent-filled content `agent-hypothesis`, cite public sources, add only permitted public-profile summaries, surface gaps — no silent provider fallback |
| Approval/decisions | Mark | human | path activation, run approval, provider-set selection per run, all external actions | all classes | n/a |

**Per-invocation requirement (charter §14.17):** Mark's prompt must name the
agent/provider for that invocation, and the run card/trace must record what was
actually used. "etc." is not a runnable manifest. The harness supplies the
invocation budget; Epoch 1 does not add a second budget system or silent
fallback.

## v0.2 segment intelligence + local research evaluation (Run 002)

Local routed tasks may classify, extract, dedupe, or cluster cleared public
research. The Mark-named external agent may research and synthesize public
sources only. Record the usefulness, traceability, and latency of local work;
the required business output is a cited segment/account/buyer/language/category
map, not a model benchmark alone. No raw customer material, scraping,
continuous monitoring, or outreach is permitted. [DR-29](../decisions/2026-07-13-segment-linked-runs.md)

## v0.3 paired validation packet for one selected test (Run 003)

`CMP-001` sent the same frozen, external-okay input to `llama3.3:70b` locally
and the current Codex/GPT-5 frontier session. Each concrete model, provider,
latency, output reference/hash, and frontier usage/cost when available is
recorded in the trace. The local arm returned no usable result twice; it was
not silently replaced. The run creates one human-reviewed packet and does not
run outreach. [DR-30](../decisions/2026-07-13-run-003-paired-model-comparison.md)

---

Note: an agent or app name does not establish a data boundary; the
provider-surface column governs routing (charter §5).
