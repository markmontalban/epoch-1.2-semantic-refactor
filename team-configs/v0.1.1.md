# Team Config v0.1.1

Last updated: 2026-07-11 00:47:16 PDT — edited by: Claude Dispatch — task: Record Run 001 approvals

Status: APPROVED by Mark 2026-07-11 (E1-R1A03, as amended — decisions/2026-07-11-run-001-approvals.md DR-15). Supersedes v0.1.

**Amendment (Mark, 2026-07-11):** Claude is **not** the default provider. The
synthesis/execution role is a combination Mark chooses — Codex, Claude, Hermes,
etc. All are **external provider surfaces**; the data-class rules are unchanged
from v0.1.

| role | actor | provider surface | allowed tasks | allowed data classes | fallback |
| --- | --- | --- | --- | --- | --- |
| Synthesis/ranking/drafting | Combination named by Mark per run — Codex, Claude, Hermes, etc. **No default provider.** Hermes excluded until Mark completes C4 (E1-G04, charter §14.7, DR-11) | Each named agent's provider is an EXTERNAL surface (Codex/OpenAI, Claude/Anthropic, Hermes' default provider) | generate angles, extract, rank, draft path cards | external-okay, or redacted-external with Mark's per-run approval; NEVER local-only | on any gap: stop and ask Mark — no silent provider fallback (charter §6) |
| Approval/decisions | Mark | human | path activation, run approval, provider-set selection per run, all external actions | all classes | n/a |

**Per-run requirement (charter §14.3):** before execution, the run card must
name the actual providers participating in that run; "etc." is not a runnable
manifest. Spend stays $0 agent-side (DR-8) — each participating provider must
run on subscription/free tier or Mark raises the cap by dated record first.

## v0.2 local-first (Run 002, planned)

Local router tasks (classify/extract/dedupe/cluster) on the local surface (may
take local-only input); the Mark-named external combo synthesizes only cleared
outputs.

## v0.3 combo-plus-dual-gate (Run 003, planned)

Adds local Gate A (logic) + Gate B (quality) critique step.

---

Note: an agent or app name does not establish a data boundary; the
provider-surface column governs routing (charter §5).
