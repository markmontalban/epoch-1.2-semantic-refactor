# Team Config v0.1

Status: DRAFT — intended for Run 001, pending Mark approval (2026-07-10)

| role | actor | provider surface | allowed tasks | allowed data classes | fallback |
| --- | --- | --- | --- | --- | --- |
| Synthesis/ranking/drafting | Claude | Anthropic (EXTERNAL surface) | generate angles, extract, rank, draft path cards | external-okay, or redacted-external with Mark's per-run approval; NEVER local-only | on any gap: stop and ask Mark — no silent provider fallback (charter §6) |
| Approval/decisions | Mark | human | path activation, run approval, all external actions | all classes | n/a |

## v0.2 local-first (Run 002, planned)

Local router tasks (classify/extract/dedupe/cluster) on the local surface (may
take local-only input); Claude synthesizes only cleared outputs.

## v0.3 claude-plus-dual-gate (Run 003, planned)

Adds local Gate A (logic) + Gate B (quality) critique step.

---

Note: an agent or app name does not establish a data boundary; the
provider-surface column governs routing (charter §5).
